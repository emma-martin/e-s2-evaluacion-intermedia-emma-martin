"use strict";const inputNumber=document.querySelector(".input"),submitBtn=document.querySelector(".btn-send"),triesCounter=document.querySelector(".meter-counter"),answersBox=document.querySelector(".info");function getRandomNumber(e){return Math.ceil(Math.random()*e)}const randomNum=getRandomNumber(100);console.log(randomNum);let tries=0;function guessNumber(e){e.preventDefault();let t=parseInt(inputNumber.value);console.log(t),tries++,triesCounter.innerHTML=`${tries}`,answersBox.innerHTML=t===randomNum?`You made it! The number is ${randomNum} and it only took you ${tries} attempts`:t>randomNum?"Too close but too high!":t<randomNum?"Too close but too low!":"Better try another game"}triesCounter.innerHTML=`${tries}`,answersBox.innerHTML="Insert a number and give it a try!",submitBtn.addEventListener("click",guessNumber);